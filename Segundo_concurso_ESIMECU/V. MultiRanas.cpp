#include <iostream>
#include <vector>
using namespace std;

int main(){
    int N;
    cin >> N;
    while(N--){
        vector<int> camino;
        int hojas;  cin >> hojas;
        while(hojas--){
            int hoja; cin >> hoja;
            camino.push_back(hoja);
        }
        for(int i = 0; i < camino.size(); ++i){

        }
    }
}
